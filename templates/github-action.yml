name: OpenAgentSecurity

on:
  pull_request:

jobs:
  oas:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - run: oas scan --base origin/main
      - run: oas gate --fail-on high

